## Backend

Express API for authentication, albums, and site settings.

### Run locally

```bash
npm install
npm run dev
```

### Scripts

- `npm run dev` - start the API with nodemon
- `npm start` - start the API with Node
- `npm run seed:admin` - create the default admin account
